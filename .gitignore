# dependências e build
node_modules/
dist/
coverage/

# ambiente — NUNCA versionar segredos (o .env.example é o modelo)
.env

# dados do Mongo local (docker compose) — só a pasta da raiz;
# src/data/ (JSONs do seed) é versionado
/data/

# diversos
*.log
.DS_Store

# exportação bruta do WhatsApp — conversa pessoal, NUNCA versionar
# (a fonte bruta fica FORA do repo: ../export-whatsapp.txt — decisão 19/08/2026)
Conversa do WhatsApp*
export-whatsapp*
